name: UI Tests

on: push

jobs:
  Tests:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false

    name: 'Cypress tests'
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Adding markdown
        run: |
        echo '### Hello world! :rocket:\
        #### Another heading' 
        >> $GITHUB_STEP_SUMMARY