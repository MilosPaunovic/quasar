name: UI Tests

on: push

jobs:
  Tests:
    runs-on: ubuntu-latest

    name: 'UI tests'
    steps:

      - name: Adding markdown
        run: echo '### Hello world! :rocket:/\n/\n#### Another heading' >> $GITHUB_STEP_SUMMARY