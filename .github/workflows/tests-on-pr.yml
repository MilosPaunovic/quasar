name: UI Tests

on: push

jobs:
  Tests:
    runs-on: ubuntu-latest

    name: 'Cypress tests'
    steps:

      - name: Adding markdown
        run: |
          ### Hello world! :rocket:
          #### Another heading 
          >> $GITHUB_STEP_SUMMARY