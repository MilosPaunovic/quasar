name: UI Tests

on: push

jobs:
  Tests:
    runs-on: ubuntu-latest

    name: 'UI tests'
    steps:

      - name: Adding markdown
        run: |
          $foo = "Hello :rocket:";
          echo $foo >> $GITHUB_STEP_SUMMARY