name: UI Tests

on: push

jobs:
  Tests:
    runs-on: ubuntu-latest

    name: 'UI tests'
    steps:

      - name: Adding markdown
        run: ${{ env.TEST }} >> $GITHUB_STEP_SUMMARY
        env:
          TEST: |
            aaa @${{ github.event.issue.user.login }}! 👋

            aaa provided an invalid or unsupported reproduction URL.
            Do not use any service other than [Codepen](https://codepen.io), [jsFiddle](https://jsfiddle.net), [Codesandbox](https://codesandbox.io), and [GitHub](https://github.com).
            Make sure the URL you provided is correct and reachable. You can test it by visiting it in a private tab, another device, etc.
            Please **edit your original post above** and provide a valid reproduction URL as explained.

            Without a proper reproduction, your issue will have to get closed.

            Thank you for your collaboration. 👏
