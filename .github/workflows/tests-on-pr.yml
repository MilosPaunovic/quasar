name: UI Tests

on: push

jobs:
  Tests:
    runs-on: ubuntu-latest

    name: 'Cypress tests'
    steps:

      - name: Adding markdown
        run: |
        echo '### Hello world! :rocket:\
        #### Another heading' 
        >> $GITHUB_STEP_SUMMARY